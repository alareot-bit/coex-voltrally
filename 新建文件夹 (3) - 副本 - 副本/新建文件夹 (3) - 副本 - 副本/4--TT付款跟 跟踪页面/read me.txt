1.	payment-tt.html：展示订单应付与批次倒计时、国家化银行收款信息（复制）、TT 水单上传/替换/删除、提交审核，提交后跳转 order.html；
	2.	order.html：订单状态与金额、批次时间轴、拼单进度、物流节点、文件区、客服入口，含 15s 轮询与指数退避。
禁止改视觉；仅加 data-* 与 JS 绑定；字段名与 API 契约保持一致。前期可用 /data/*.json mock，后端就位后仅替换 fetch 源。按各页 QA 清单自测后提交 dist.zip。
